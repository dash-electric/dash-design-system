name: Staging Deployment

# Deploys docs site + registry API to Cloud Run on push to main.
on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  REGION: asia-southeast2
  REPOSITORY: cloudrun-standard
  GOOGLE_PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
  SERVICE_NAME: dash-ds-registry-staging

jobs:
  build-image:
    name: Build Image and Push to Artifact Registry
    runs-on: ubuntu-latest
    environment: staging

    permissions:
      contents: read
      actions: write
      packages: write
      id-token: write

    outputs:
      image_tag: ${{ steps.set-output.outputs.image_tag }}
      commit_hash: ${{ steps.commit.outputs.hash }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set Commit Hash
        id: commit
        run: echo "hash=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Authenticate with Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.NEXT_GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v3
        with:
          project_id: ${{ env.GOOGLE_PROJECT_ID }}

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ${{ env.REGION }}-docker.pkg.dev/${{ env.GOOGLE_PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:${{ steps.commit.outputs.hash }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Set Output for Deploy Job
        id: set-output
        run: |
          IMAGE_TAG="${{ env.REGION }}-docker.pkg.dev/${{ env.GOOGLE_PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:${{ steps.commit.outputs.hash }}"
          echo "image_tag=$IMAGE_TAG" >> $GITHUB_OUTPUT

  deploy:
    name: Deploy to Cloud Run (Staging)
    runs-on: ubuntu-latest
    needs: build-image
    environment: staging

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Authenticate with Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.NEXT_GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v3
        with:
          project_id: ${{ env.GOOGLE_PROJECT_ID }}

      - name: Create Environment File
        run: |
          cat > env.yaml << 'EOF'
          APP_ENV: "staging"
          APP_PORT: "3000"
          APP_NAME: "${{ env.SERVICE_NAME }}"
          APP_VERSION: "${{ needs.build-image.outputs.commit_hash }}"
          NODE_ENV: "production"
          DASH_REGISTRY_TOKEN: "${{ secrets.DASH_REGISTRY_TOKEN }}"
          SITE_URL: "${{ vars.STG_SITE_URL }}"
          EOF

      - name: Deploy to Cloud Run (Staging)
        run: |
          echo "Deploying image: ${{ needs.build-image.outputs.image_tag }}"
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image ${{ needs.build-image.outputs.image_tag }} \
            --region ${{ env.REGION }} \
            --platform managed \
            --allow-unauthenticated \
            --min-instances 0 \
            --memory 256Mi \
            --cpu 1 \
            --port 3000 \
            --env-vars-file env.yaml
