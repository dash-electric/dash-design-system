{
  "name": "@dash/worker",
  "version": "0.0.1",
  "description": "Hermes — autonomous gap-to-vendored generation worker for Dash DS. Reads gap queue, generates block via Anthropic SDK with Skill v2 context, validates against foundation rules, and opens a PR.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "dash-worker": "./dist/index.js"
  },
  "files": [
    "src",
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@dash/skill": "workspace:*",
    "kleur": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
