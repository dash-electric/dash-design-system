{
  "name": "@dash/registry-schema",
  "version": "0.0.1",
  "description": "Shared registry types and schemas across Dash DS repo packages (stub).",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
