{
  "name": "@dash/skill",
  "version": "0.0.1",
  "description": "Claude Code skill that auto-injects Dash Design System context into AI working memory. SCAFFOLD ONLY — Phase 2 (week 3, post-pilot). Manifest + structure; runtime content injection lands later.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "files": [
    "src",
    "manifest.json",
    "SKILL.md",
    "README.md"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
