# Dash Build Handoff — 2026-05-25

Use this as the starting context for a new Codex chat continuing Dash Build.

## Full Prompt For New Chat

```text
You are continuing Dash Build inside /Users/irfanprimaputra.b/dash-ds.

Context:
- Dash Build is a local Lovable-for-Dash builder for improving existing Dash repos using Dash DS governance.
- Main package: packages/dash-build.
- Current local dashboard: http://127.0.0.1:7801/dashboard when daemon is running.
- Production Dash repos under /Users/irfanprimaputra.b/Dash/* are READ-ONLY references. Do not edit production repos directly.
- Dash DS rules come from AGENTS.md, design.md, LAYERED-ARCHITECTURE.md, apps/docs/registry/rules, and @dash/skill.
- Layer 0 is locked. Layer 1 primitives should be reused first. New components are allowed only when existing ones do not cover the need, and must be marked as component candidate / DS gap.

Current implementation status:
- OpenAI/Codex auth works locally. User logged in through Codex CLI.
- BYO OpenAI API key path exists as a team-friendly fallback.
- GitHub is optional for local preview. GitHub App is only needed for publish/PR.
- Repo dropdown should only show valid repos such as dash/portal-v2 and dash/backoffice. dash/halo-dash-fe was treated as an outlier and should stay removed unless product direction changes.
- Dash Build can reset local prompt/run history with POST /api/prompts/reset or the dashboard Reset button.
- Generated artifacts persist as prompt runs and preview bundles in ~/.dash-build.
- Current clean milestone commit: 5054740 feat(dash-build): add repo-aware preview harness.

What was fixed:
1. Codex/OpenAI auth replaced the previous Claude-only blocker.
2. Dash Build no longer requires GitHub to test local generation/preview.
3. Clarification was tightened so obvious repo facts are inferred from selected repo instead of asking users questions they cannot answer.
4. Repo Context Pack is injected into generation:
   - selected repo
   - inferred surface/audience/theme
   - known shell nav
   - default route
   - target route
   - target nav label
   - integration contract
   - mock-data-only data policy
5. Raw hex/token violations no longer pass validation.
6. Preview fallback no longer becomes a blank screen; it shows file list, validation, and errors if iframe bundling fails.
7. Dash Build now owns a deterministic repo-aware preview harness:
   - preview.tsx renders only generated route/content
   - Dash Build wraps it with selected repo shell/nav/active route
   - the model must not redraw sidebar/topbar/nav from memory
8. Preview shell now includes a minimal utility CSS subset so generated Tailwind-like classes render in the sandbox instead of falling back to browser-default buttons/tables.
9. Obsidian canonical plan was updated with P0D Preview Truthfulness Checkpoint.

Verified P0 test:
- Prompt id: prm_13a642f2-ca2
- Repo: dash/backoffice
- Prompt: "Tambahin compact delivery exception filter di backoffice delivery page. Operator bisa filter status problem delivery, lihat 3 KPI kecil, dan review daftar order bermasalah pakai mock data. Harus tetap di existing delivery shell dan jangan bikin sidebar baru."
- Target route: /delivery
- Target nav: Orders
- Validation: passed
- Foundation score: 100/100
- Preview mode: component iframe live
- Visual result: Dash Build-owned Backoffice shell with Orders active, generated delivery exception content mounted inside the route slot.

Recent verification commands:
- pnpm --filter @dash/build typecheck
- pnpm --filter @dash/build test -- src/preview/__tests__/bundler.test.ts src/skills/__tests__/chain.test.ts src/skills/__tests__/validator.test.ts src/pipeline/__tests__/orchestrator.test.ts
- pnpm --filter @dash/build build

Open product direction:
- Dash Build should become a multi-project workspace, not a single endless chat.
- Product objects: Workspace -> Project -> Thread -> Run -> Context Pack -> Artifact.
- P0 focus stays on generate-to-preview for existing Dash repos.
- Publish, owner dashboard, team permissioning, usage governance, and API-key business controls are P1/P2 until P0 is stable.
- Future team version should let normal users use the front UI without cloning private FE/BE repos. Owner/admin connects GitHub/repos; workers use controlled preview/publish flows.

Known gaps and what to improve next:
1. Repo preview is better but still not pixel-identical to real backoffice/portal.
   Next: build per-repo preview adapters that mirror backoffice and portal-v2 shells/routes more closely without production auth bypass.
2. Auth-gated real apps still cannot show protected pages in iframe without a real session.
   Next: use first-party preview harness with mocked auth/context/fixture data for review; keep "Open real app" for manual authenticated testing.
3. Generated UI can still be too generic if model ignores existing patterns.
   Next: strengthen prompt composer and validation to detect duplicate shells, weak styling, and output that fails to respect selected route/nav.
4. Need fresh reset test with a different prompt, not the delivery exception prompt.
   Next: reset dashboard, submit a new backoffice or portal-v2 prompt, inspect whether preview respects existing repo context and whether design output is acceptable.
5. Need project/home model after P0:
   Home should show projects, recent threads, running jobs, and preview/publish status like a real builder workspace.
6. Need file/image prompt context later.
   Users will type rough thoughts and may attach screenshots/files; context pack should absorb those before generation.
7. Need publish button later.
   Publish means owner-approved PR/deploy, not just local preview.
8. Need owner/admin dashboard later.
   Owner sees published outputs, user activity, usage/cost, permissions, and merge approvals.

Immediate next action:
- Keep daemon at http://127.0.0.1:7801/dashboard if needed.
- Start P0.1 Repo Preview Adapter:
  1. Backoffice adapter: deterministic shell closer to real /delivery, /provider, /broadcast, /pitstop.
  2. Portal-v2 adapter: deterministic shell closer to /en/deliveries, /en/billing, /en/users.
  3. Add validation rule that preview.tsx must not include its own sidebar/topbar when repoContext.existingShell = true.
  4. Reset and run a new prompt that is different from "Performance Mitra" and "Delivery exception".
  5. Compare screenshot: selected repo shell, active nav, route, generated content, styling, states.

Important tone from user:
- Be direct and critical. Do not just agree.
- User writes rough prompts; the product must infer context and ask only useful clarification.
- Focus on Dash Build core until generate -> preview feels done.
- Keep Obsidian updated so new chats can resume without losing the thread.
```

## Done

- Codex/OpenAI path is the active local generation path.
- BYO API key path remains as broader team fallback.
- GitHub is not a blocker for preview; it is only a blocker for publish/PR.
- Repo list cleaned so the outlier repo does not lead the test flow.
- Context Pack is now a first-class part of generation and review.
- `design.md` is loaded into the skill chain and now says Dash Build owns repo-aware preview shell wrapping.
- Preview no longer relies on the model to redraw app chrome.
- Validation catches raw hex/token mistakes.
- Fresh P0 prompt generated and rendered in repo-aware preview.
- Checkpoint committed in git: `5054740 feat(dash-build): add repo-aware preview harness`.

## Not Done

- Preview is not yet pixel-identical to the real repo.
- Real protected backoffice/portal pages still need real auth for manual top-level review.
- No production auth bypass should be added.
- Publish button/PR flow is not the focus of this milestone.
- No owner/admin dashboard yet.
- No multi-user workspace/project/thread persistence model yet beyond local run history.
- No image/file prompt ingestion yet.
- No full project home/canvas list yet.

## What Next

1. Build P0.1 repo preview adapters for `backoffice` and `portal-v2`.
2. Add duplicate-shell validation for generated previews.
3. Reset Dash Build and run a fresh prompt that targets a different feature.
4. Use screenshots to judge:
   - repo shell fidelity
   - route/nav respect
   - generated content quality
   - design-token usage
   - state coverage
5. Only after this feels stable, move to Home/Project/Thread model and Publish.

## Current Local Commands

```bash
cd /Users/irfanprimaputra.b/dash-ds
pnpm --filter @dash/build typecheck
pnpm --filter @dash/build test
pnpm --filter @dash/build build
DASH_BUILD_PORT=7801 node packages/dash-build/dist/daemon.js
```

Reset local test history:

```bash
curl -X POST http://127.0.0.1:7801/api/prompts/reset
```

