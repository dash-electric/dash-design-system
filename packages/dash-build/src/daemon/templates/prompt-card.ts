/**
 * @deprecated — use ./components/prompt-card.js. Kept as a thin re-export so
 * earlier import paths continue to resolve while consumers migrate.
 */
export {
  renderPromptCard,
  renderPromptList,
} from "./components/prompt-card.js"
